{
  "stylelint": {
    "extends": "stylelint-config-standard",
    "rules": {
      "no-descending-specificity": null
    }
  },
  "scripts": {
    "lint": "stylelint  --fix src/"
  },
  "pre-commit": [
    "lint"
  ],
  "dependencies": {
    "pre-commit": "^1.2.2",
    "stylelint": "^10.1.0"
  },
  "devDependencies": {
    "stylelint-config-standard": "^18.3.0"
  }
}
